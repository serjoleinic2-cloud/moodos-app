ЧТО Я БЫ ДОЖАЛ ПЕРЕД РЕЛИЗОМ (ПОСЛЕДНИЙ СЛОЙ)

Это не рефакторинг — это полировка стабильности

🔴 1. Проверить: ВСЕ analytics реально используют period

Ты сделал _filterByDays — но важно:

👉 Убедись, что ВСЕ функции его используют

Проверь:

getEffectivenessRate ✅
getAverageMoodLift ✅
getEffectivenessByState ✅

❗ Если хоть одна осталась без фильтра → снова рассинхрон

🔴 2. avgMood в insight.js

Claude уже подсветил раньше:

history.reduce(...)

👉 если это не через calcHistory (filtered)
→ баг остался

🟡 3. systemState.isReady (очень частый скрытый баг)

Проверь:

если экран открывается до isReady → он не зависает?

Если сейчас:

if (!isReady) return;

👉 это плохо (может остаться пустой экран)

✔ лучше:

либо retry
либо show loader
🟡 4. i18n пустые ключи

Сейчас:

t(key)

если нет ключа → пусто

👉 добавь ОДИН лог (dev only):

if (!result) console.warn('[i18n missing]', key);

Это спасёт тебе часы в будущем

🟡 5. backup лимит (ответ на твой старый вопрос)

Ты спрашивал:

“что будет при 30/30 backups?”

👉 должно быть:

добавляется новый
старый удаляется (FIFO или по дате)

Проверь:

slice(0, limit)

✔ если так → всё ок