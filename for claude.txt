TASK 147 — CLAUDE FINAL PRE-RELEASE AUDIT (CRITICAL)

Скопируй это полностью в Claude 👇

🔴 CONTEXT
Project: Neyra App
Platform: Android WebView (Java + JS) + Firebase Firestore
Architecture:
- Offline-first (localStorage)
- Optional cloud sync (Firebase)
- Anonymous Auth (Firebase)
- Premium gating
- Offline AI (rule-based)

Snapshot file: NEYRA_AUDIT_SNAPSHOT.txt
🔴 GOAL

Провести production-level аудит перед релизом.

Не обзор. Не рекомендации.
👉 Найти реальные риски:

потери данных
утечки данных
финансовые риски
деградация через время
race conditions
баги масштабирования
🔴 AUDIT SCOPE (ОБЯЗАТЕЛЬНО)
1. 🔐 Firebase / Cloud Architecture

Проверь:

Есть ли user isolation (uid)
Где хранится uid
Может ли пользователь получить чужие данные
Есть ли риск перезаписи чужих данных
Firestore структура (documents vs collections)
Есть ли unbounded growth
payload size (1MB limit)
ошибки при отсутствии интернета
retry / fail logic
silent failures

👉 Вывод:

SAFE / NOT SAFE
точка поломки
2. 💾 Data Storage (localStorage lifecycle)

Проверь ВСЕ хранилища:

mood_history
notes_history
reflections
voice_history
photo_history
neyra_patterns
user_profile

Оцени:

лимиты
рост через 1 год / 2 года
quota overflow (5–10MB WebView)
JSON.parse на больших объёмах
деградация UI
потеря данных при overflow
3. ⚡ Sync Logic (Cloud Sync + Restore)

Проверь:

race conditions (bridge, app init)
debounce logic
может ли sync потеряться
overwrite logic (local vs cloud)
partial restore
конфликт версий данных
4. 👑 Premium System (Security Audit)

Проверь:

можно ли обойти premium
зависимость от window переменных
persistence premium статуса
expiry логика
sync только для premium (можно ли обойти)
5. 🤖 AI / Insight Engine

Проверь:

деградация при 1000+ записей
mergePatterns growth
точность при timeBucket
влияние старых данных
bias / неправильные рекомендации
6. 📱 UX Critical Flows

Проверь:

потеря данных при закрытии приложения
первый запуск (onboarding + restore)
поведение без интернета
fallback если Firebase не работает
7. 🔐 Privacy / GDPR

Проверь:

соответствует ли PRIVACY.md реальной логике
есть ли скрытый сбор данных
пользовательский контроль данных
удаление данных
🔴 OUTPUT FORMAT (ОЧЕНЬ ВАЖНО)

Строго:

🔴 CRITICAL (Release blockers)
описание
где в коде
что сломается
FIX (код)
🟡 MEDIUM (Must fix before scale)

...

🟢 LOW (Cleanup / tech debt)

...

🚀 FINAL VERDICT
SAFE TO RELEASE / NOT SAFE
почему
что сломается первым
🔴 EXTRA (ОБЯЗАТЕЛЬНО)

Ответь отдельно:

1. Что сломается через 30 дней?
2. Что сломается через 6 месяцев?
3. Где будет первая потеря данных?
4. Где будет первая утечка данных?
5. Где начнутся реальные расходы ($)?