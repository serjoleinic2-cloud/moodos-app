# 🧠 TASK — FULL SYSTEM AUDIT (POST-REFACTOR DAMAGE)

## CONTEXT

Проект: мобильное приложение (Capacitor / JS SPA)

После серии правок другим агентом (OpenCode) система начала вести себя некорректно.

⚠️ ВАЖНО:
НЕ ПРЕДЛАГАЙ РЕФАКТОРИНГ С НУЛЯ
НЕ МЕНЯЙ АРХИТЕКТУРУ
ЗАДАЧА — НАЙТИ ПОЛОМКИ И ДАТЬ ТОЧЕЧНЫЕ ФИКСЫ

---

# 🔴 BLOCK 1 — HOME SCREEN LOGIC BROKEN

## Проблемы:

1. Таблица закономерностей (patterns) врёт:

* пользователь отмечал "сон" > 4 раз
* система упорно показывает "кофе"

👉 Нужно:

* проверить алгоритм подсчёта факторов
* проверить веса / агрегацию
* проверить сортировку

---

2. Дневная рефлексия:

* кнопка "Начать запись" работает
* НО:

  * нет обратного отсчёта записи
  * исчез старый таймер
  * добавлен новый таймер 3→2→1 (лишний)

👉 Нужно:

* найти где удалён оригинальный countdown
* где добавлен новый (и почему)
* восстановить правильное поведение

---

# 🔴 BLOCK 2 — APP CORE DAMAGE

## Проблема:

OpenCode изменял `app.js`, затем заявил что "контракта нет"

👉 Нужно:

1. Проверить:

* глобальные переменные
* security layer (_billingPremium)
* init sequence

2. Найти:

* нарушенные контракты между модулями
* дублирующиеся init (DOMContentLoaded вызывается дважды)

---

# 🔴 BLOCK 3 — MEDITATION / PLAYER REGRESSION

## Проблемы:

* плейер не работает
* иконки были заменены глобально

👉 Нужно:

* проверить:

  * инициализацию audio
  * lifecycle (onEnter / onExit)
  * src треков
* проверить:

  * соответствие UI и состояния плеера

---

# 🔴 BLOCK 4 — INSIGHT / PREMIUM LOGIC

## Проблема:

* кнопка "Премиум" должна открывать больше закономерностей
* но сейчас они уже доступны бесплатно

👉 Нужно:

* проверить:

  * где фильтруются premium insights
  * используется ли isPremium()
  * нет ли bypass логики

---

# 🔴 BLOCK 5 — PDF REPORT ISSUES

## 1. Локализация:

* "rest" не переведён (в "Что повлияло")

---

## 2. Статистика:

Проблема:

* % отображаются хаотично (вёрстка сломана)

👉 Требование:

* % ВСЕГДА:

  * в левом верхнем углу блока
* текст:

  * ниже

---

## 3. Критические значения:

Если:

* 0% → объяснить ПОЧЕМУ
* 100% → объяснить ПОЧЕМУ

👉 нужно:

* добавить интерпретацию

---

## 4. Рекомендации:

Проблема:

```id="badrec"
"Хороший средний показатель 72%"
```

👉 это НЕ рекомендация

👉 нужно:

* найти генератор рекомендаций
* проверить логику
* предложить улучшение:

  * action-based
  * причинно-следственное

---

## 5. Практики:

* "поддержки" → должно быть "Поддержки"
* delta настроения = всегда 0

👉 проверить расчёт delta

---

## 6. Журнал:

* "текущее состояние" = прочерки

👉 либо:

* данные не передаются
* либо UI сломан

---

# 🔴 BLOCK 6 — DATA CONSISTENCY

Проверить:

* история (history)
* инсайты (insight)
* PDF

👉 используют ли они ОДИН И ТОТ ЖЕ источник данных

---

# 🔴 BLOCK 7 — ROOT CAUSE ANALYSIS

Ответь:

1. Какие изменения OpenCode сломали систему?
2. Где нарушены контракты?
3. Где появились race conditions?
4. Где логика была переписана неправильно?

---

# 📌 OUTPUT FORMAT

Дай строго:

## 1. CRITICAL BUGS (с файлами и причинами)

## 2. LOGIC ERRORS

## 3. UI/UX REGRESSIONS

## 4. DATA FLOW BREAKS

## 5. MINIMAL FIX PLAN (без переписывания системы)

---

# 🚫 ЗАПРЕЩЕНО

* переписывать архитектуру
* "лучше сделать заново"
* абстрактные советы

---

# 🎯 ЦЕЛЬ

Вернуть систему в:

* логически корректное состояние
* стабильный runtime
* согласованную аналитику

---

END TASK
