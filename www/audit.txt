AUDIT TASK FOR CLAUDE — FULL APP BEHAVIOR + FLOW REVIEW
🎯 ЦЕЛЬ АУДИТА

Провести полный анализ приложения moodos-app на уровне:

UI → state → data flow
событий (click / input / navigation)
хранения данных
аналитики (insight/report)
календаря
настроек и backup
premium логики (если есть)
🧠 ГЛАВНАЯ ЗАДАЧА

Нужно проверить НЕ код как стиль, а:

❗ “что реально происходит при использовании приложения”

🔍 ЧТО ИМЕННО НУЖНО ПРОЙТИ

Claude должен пройти ВСЕ СЦЕНАРИИ:

🟢 1. HOME SCREEN (ГЛАВНАЯ)

Проверить:

slider изменения настроения
реакция аватара
кнопка OK (сохранение)
что происходит при:
первом входе
повторном входе
переключении экранов и возврате
❗ ВАЖНО НАЙТИ:
рассинхрон UI и state
случаи когда UI не обновляется до re-render
зависимости от навигации вместо реактивности
🟢 2. INSIGHT SCREEN

Проверить:

все периоды (7/30/90/365)
сравнение данных
empty states
fallback тексты (особенно i18n keys)
корректность label периода
❗ НАЙТИ РИСКИ:
когда данные отсутствуют
когда данные частичные
когда сравнение невозможно
когда UI показывает raw keys
🟢 3. REPORT SCREEN

Проверить:

календарь (tap interaction)
popup логика
отображение практик
mood % отображение
multiplier (x1/x2)
❗ НАЙТИ:
нет ли дублирования данных
нет ли несогласованности структуры day data
все ли дни имеют одинаковую модель данных
🟢 4. CALENDAR SYSTEM

Проверить:

обработку событий по дням
агрегацию практик
корректность подсчётов
отсутствие duplicate entries
🟢 5. SETTINGS / BACKUP

Проверить:

создание backup
лимиты хранения (если есть 1/30 и т.д.)
что происходит при превышении лимита
restore flow
i18n тексты (нет ли raw keys)
🟢 6. DATA LAYER (КРИТИЧНО)

Проверить:

где данные raw events
где normalized data
где UI-ready data
есть ли единый слой трансформации
⚠️ ГЛАВНАЯ ЗАДАЧА (САМАЯ ВАЖНАЯ)

Claude должен найти:

❗ “разрывы цепочек данных”

Примеры:

UI изменился → state не обновился
state изменился → UI не обновился
данные есть → но UI считает их пустыми
данные частично есть → fallback “no data”
разные экраны используют разные структуры одного и того же объекта
🔮 БУДУЩИЕ БАГИ (PREDICTIVE AUDIT)

Claude должен отдельно перечислить:

⚠️ “возможные будущие баги”

Например:

где может сломаться при увеличении данных
где пустые значения приведут к UI crash
где i18n может деградировать в raw key
где масштабирование (1000+ записей) сломает логику
где разные экраны разъедутся по data model
🧪 NAVIGATION FLOW TEST

Claude должен мысленно пройти:

open app
change mood slider
go to report
go to insight
go back home
repeat actions
simulate restart app
📌 OUTPUT ОЖИДАЕМЫЙ ОТ CLAUDE

Claude должен вернуть:

1. CRITICAL ISSUES
ломает UX / данные
2. HIGH RISK ISSUES
может сломаться в будущем
3. DATA FLOW PROBLEMS
где теряются данные
4. UI/STATE DESYNC POINTS
где UI не отражает state
5. RECOMMENDED FIXES
короткие точечные решения
🚫 ЧТО НЕ НУЖНО
не переписывать код полностью
не предлагать “архитектуру будущего”
не рефакторить ради стиля
не менять UI без причины
💬 КОРОТКО

“Проверь приложение как пользователь + как data engineer + как QA. Найди, где оно может сломаться сейчас и в будущем.”