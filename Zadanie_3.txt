TASK — AUDIT Z3 (REFLECTION RESPONSE OVERRIDE)
КОНТЕКСТ

Проект: Neyra App
Проблема: “Дневная рефлексия → Быстрый инсайт”

❗ СИМПТОМ
Пользователь вводит текст
Нажимает “Быстрый инсайт”
Ответ:
появляется на долю секунды ✅
затем заменяется обратно на "Анализирую..." ❌
✅ ЧТО УЖЕ ИСПРАВЛЕНО
❌ Удалён двойной GENERATE_INSIGHT из SAVE_NOTE
✅ Только один вызов safeGenerateInsight()
✅ window._reflectionBusy защита
✅ _activeInsightId защита от гонок
✅ защита от setLoading overwrite
✅ dataset.bound от двойного bind
❗ ВАЖНО

Это НЕ race condition.

👉 Ответ УСПЕВАЕТ отрендериться → значит:

safeGenerateInsight работает
UI рендер работает

❗ Но потом:
→ что-то снова вызывает setLoading(true)
ИЛИ
→ происходит повторный render()

📦 ДАННЫЕ ДЛЯ АНАЛИЗА
ФАЙЛЫ:
app.js
offline-ai.js
system-core.js
home.js (если влияет на UI)
insight.js (если есть общий render)
🔍 ЧТО НУЖНО НАЙТИ
1. ВСЕ места где вызывается:
setLoading(true)
setLoading(false)
responseEl.innerText =
renderInsight()

👉 Найти:

кто вызывает setLoading(true) ПОСЛЕ успешного ответа
2. Проверить ПОЛНЫЙ FLOW:
click → analyzeNoteBtn
  → setLoading(true)
  → safeGenerateInsight()
  → render response
  → ❌ что-то снова делает setLoading(true)
3. Проверить:
A. Есть ли:
setInterval
requestAnimationFrame
observer / watcher

👉 которые могут перерисовывать UI

B. Есть ли повторный bind:
addEventListener вызывается 2 раза
даже несмотря на dataset.bound
C. Есть ли:
responseEl.innerHTML = ...

в других местах (НЕ в рефлексии)

4. Проверить offline-ai.js

👉 Может ли safeGenerateInsight:

возвращать undefined
триггерить fallback
вызываться повторно
5. Проверить system-core.js

👉 Есть ли:

скрытые dispatch после SAVE_NOTE
авто-инсайты
события типа AVATAR_UPDATE, которые трогают UI
🎯 ЗАДАЧА

Найти ТОЧНОЕ место, которое:

👉 перезаписывает responseEl ПОСЛЕ render ответа

📋 ФОРМАТ ОТВЕТА

Ожидаю:

1. ROOT CAUSE (конкретная строка/файл)
2. Почему происходит перезапись
3. Минимальный фикс (НЕ переписывать архитектуру)
4. Альтернатива (если есть)
🚫 ВАЖНО
НЕ давать общие советы
НЕ переписывать код
НЕ предлагать “попробуйте debounce”

👉 Нужен конкретный источник перезаписи UI

🧠 ПОДСКАЗКА

Ответ уже появляется → значит баг НЕ в генерации
👉 баг в повторном обновлении UI